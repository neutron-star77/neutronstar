---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';
import PostCard from '../components/PostCard.astro';
import { SITE, GUITAO } from '../config';

const allBlog = (await getCollection('blog')).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
const allProjects = (await getCollection('project')).sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
const recentBlog = allBlog.slice(0, 6);
const recentProjects = allProjects.slice(0, 4);
---

<Base>
	<section class="section">
		<div class="section-head">
			<h2 class="section-title">最新杂谈</h2>
			<a class="section-more" href="/blog">全部 →</a>
		</div>
		<div id="post-list-container" class="post-list-container grid-mode">
			{recentBlog.map((p, i) => (
				<div class="post-card-item">
					<PostCard
						href={`/blog/${p.id}`}
						title={p.data.title}
						description={p.data.description}
						date={p.data.pubDate}
						tags={p.data.tags}
						cover={p.data.cover || GUITAO[i % GUITAO.length]}
						kind="文章"
					/>
				</div>
			))}
		</div>
	</section>

	<section class="section">
		<div class="section-head">
			<h2 class="section-title">星舰项目</h2>
			<a class="section-more" href="/projects">全部 →</a>
		</div>
		<div id="post-list-container" class="post-list-container grid-mode">
			{recentProjects.map((p, i) => (
				<div class="post-card-item">
					<PostCard
						href={`/projects/${p.id}`}
						title={p.data.title}
						description={p.data.description}
						date={p.data.date}
						tags={p.data.tags}
						cover={p.data.cover || GUITAO[(i + 5) % GUITAO.length]}
						kind="项目"
					/>
				</div>
			))}
		</div>
	</section>
</Base>

<style>
	.section { margin-top: 2.5rem; }
	.section-head {
		display: flex;
		align-items: baseline;
		justify-content: space-between;
		margin-bottom: 1.4rem;
	}
	.section-title { font-size: 1.5rem; margin: 0; color: var(--deep-text); }
	.section-more { color: var(--primary); font-size: 0.9rem; text-decoration: none; }
	.section-more:hover { text-decoration: underline; }
</style>
